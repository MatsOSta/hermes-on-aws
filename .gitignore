# OpenTofu working data and generated state/plans (lockfiles stay tracked)
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
*.plan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Secrets and local credentials
.env
.env.*
!.env.example
*.pem
*.key
credentials
secrets/

# Editor and operating-system files
.DS_Store
.idea/
.vscode/
*.swp
*~

# Runtime data belongs on the host, never in Git
var/lib/hermes/

